@model IEnumerable<DoAnCoSo.Models.Order>
@using DoAnCoSo.Extensions

@{
    ViewData["Title"] = "Quản lý đơn hàng";
}

<div class="container mt-5">
    <h2 class="mb-4 text-primary">📦 Quản lý đơn hàng</h2>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">@TempData["SuccessMessage"]</div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
    }

    @if (!Model.Any())
    {
        <div class="alert alert-info">Không có đơn hàng nào trong hệ thống.</div>
    }
    else
    {
        <div class="row row-cols-1 g-4">
            @foreach (var order in Model)
            {
                <div class="col">
                    <div class="card shadow-sm border-0">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <div>
                                    <h5 class="card-title mb-1">Mã đơn: <span class="text-dark fw-bold">@order.Id</span></h5>
                                    <small class="text-muted">Người đặt: @order.User?.FullName</small><br />
                                    <small class="text-muted">Ngày đặt: @order.OrderDate.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</small>
                                </div>
                                <div class="text-end">
                                    <div><strong class="text-dark">Tổng tiền:</strong> <span class="text-danger fw-bold">@order.TotalPrice.ToString("N0") VNĐ</span></div>
                                    <div>
                                        <strong>Trạng thái:</strong>
                                        @if (order.Status == DoAnCoSo.Models.OrderStatusEnum.ChoXacNhan)
                                        {
                                            <span class="badge bg-warning text-dark">Chờ xác nhận</span>
                                        }
                                        else if (order.Status == DoAnCoSo.Models.OrderStatusEnum.DaXacNhan)
                                        {
                                            <span class="badge bg-success">Đã xác nhận</span>
                                        }
                                        else if (order.Status == DoAnCoSo.Models.OrderStatusEnum.DaHuy)
                                        {
                                            <span class="badge bg-secondary">Đã hủy</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-light text-dark">@order.Status.GetDisplayName()</span>
                                        }
                                    </div>
                                </div>
                            </div>

                            <hr />

                            <div class="mb-3">
                                <strong>Chi tiết sản phẩm:</strong>
                                @if (order.OrderDetails != null && order.OrderDetails.Any())
                                {
                                    <ul class="list-group list-group-flush">
                                        @foreach (var item in order.OrderDetails)
                                        {
                                            <li class="list-group-item">
                                                <div class="d-flex justify-content-between">
                                                    <div>
                                                        <strong>@item.Product.Name</strong>
                                                        @if (!string.IsNullOrEmpty(item.SelectedFlavor))
                                                        {
                                                            <span class="text-muted">(Vị: @item.SelectedFlavor)</span>
                                                        }
                                                    </div>
                                                    <div>@item.Quantity x @item.Price.ToString("N0") VNĐ</div>
                                                </div>
                                            </li>
                                        }
                                    </ul>
                                }
                                else
                                {
                                    <p class="text-muted">Không có chi tiết sản phẩm.</p>
                                }
                            </div>

                            <div class="text-end">
                                <a asp-action="Details" asp-route-id="@order.Id" class="btn-detail">
                                    <i class="bi bi-eye"></i> Xem chi tiết
                                </a>

                                @if (order.Status == DoAnCoSo.Models.OrderStatusEnum.ChoXacNhan)
                                {
                                    <form asp-action="Confirm" asp-route-id="@order.Id" method="post" class="d-inline"
                                          onsubmit="return confirm('Bạn có chắc chắn muốn xác nhận đơn hàng này?');">
                                        <button type="submit" class="btn-accept">
                                            <i class="bi bi-check2-circle"></i> Xác nhận
                                        </button>
                                    </form>

                                    <form asp-action="Cancel" asp-route-id="@order.Id" method="post" class="d-inline"
                                          onsubmit="return confirm('Bạn có chắc chắn muốn hủy đơn hàng này?');">
                                        <button type="submit" class="btn-cancel">
                                            <i class="bi bi-x-circle"></i> Hủy đơn hàng
                                        </button>
                                    </form>
                                }

                                @* <form asp-action="Delete" asp-route-id="@order.Id" method="post" class="d-inline"
                                      onsubmit="return confirm('Bạn CÓ CHẮC CHẮN muốn xóa đơn hàng này vĩnh viễn?');">
                                    <button type="submit" class="btn btn-danger btn-sm">
                                        <i class="bi bi-trash3"></i> Xóa
                                    </button>
                                </form> *@
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

<style>
    /* Badge trạng thái lớn hơn */
    .badge {
        font-size: 12px; /* tăng chữ */
        padding: 8px 8px; /* tăng padding */
    }

    /* Nút Xem chi tiết nhỏ hơn */
    .btn-detail {
        display: inline-block;
        text-align: center;
        background-color: #FFB6C1;
        color: #000;
        font-weight: bold;
        padding: 8px 12px; /* giảm padding */
        font-size: 12px; /* giảm chữ */
        border-radius: 25px;
        text-decoration: none;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

        .btn-detail:hover {
            background-color: #FF6185;
            color: #fff;
        }

    .btn-cancel {
        display: inline-block;
        text-align: center;
        border: 2px solid red;
        color: red;
        font-weight: bold;
        padding: 8px 12px; /* giảm padding */
        font-size: 12px; /* giảm chữ */
        border-radius: 25px;
        text-decoration: none;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

        .btn-cancel:hover {
            background-color: red;
            color: #fff;
        }

    .btn-accept {
        display: inline-block;
        text-align: center;
        border: 2px solid green;
        color: green;
        font-weight: bold;
        padding: 8px 12px; /* giảm padding */
        font-size: 12px; /* giảm chữ */
        border-radius: 25px;
        text-decoration: none;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

        .btn-accept:hover {
            background-color: green;
            color: #fff;
        }
</style>